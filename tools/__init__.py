"""Research, harness, and ops scripts.

Not used by the API or the core pipeline — these scripts are run by hand
from the project root (e.g. ``py -3 tools/regression.py``). They depend on
``vectorize`` and ``core/`` and on the ``tests/`` harness data.
"""
